suites:
    api:
        namespace: SDK
        psr4_prefix: SDK
        src_path: %paths.config%/src/client
        spec_path: %paths.config%/tests/client

    domain:
        namespace: Gousto
        psr4_prefix: Gousto
        src_path: %paths.config%/src/domain
        spec_path: %paths.config%/tests/domain

    infrastructure:
        namespace: Infrastructure
        psr4_prefix: Infrastructure
        src_path: %paths.config%/src/infrastructure
        spec_path: %paths.config%/tests/infrastructure